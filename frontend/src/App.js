import { useEffect, useRef, useState } from "react";
import {
  Bell, ChevronDown, Hash, Headphones, Inbox, Laptop, LockKeyhole,
  LogIn, Menu, MessageSquare, Mic, MonitorUp, MoreHorizontal, Plus,
  Search, Send, Settings, Shield, Signal, Smile, UserPlus, Users, Volume2,
  X, Zap
} from "lucide-react";
import "@/App.css";
import "@/Voice.css";
import { api, clearSession, saveSession, websocketUrl } from "@/lib/api";

const communities = [
  { id: "nexus", label: "N", name: "Nexus Lab", color: "orange" },
  { id: "atlas", label: "A", name: "Atlas Studio", color: "mint" },
  { id: "orbit", label: "O", name: "Orbit", color: "yellow" },
];
const channels = [
  { id: "general", label: "general", count: "24" },
  { id: "projects", label: "projects", count: "8" },
  { id: "feedback", label: "feedback", count: "" },
];
const initialMessages = [
  { id: 1, name: "Maya Chen", initials: "MC", role: "Product", time: "09:42", text: "Bom dia, equipe. O novo fluxo de onboarding já está no ar para revisão.", tone: "orange" },
  { id: 2, name: "Ravi Mendes", initials: "RM", role: "Design", time: "09:45", text: "Vou olhar agora. A nova hierarquia deixa as ações principais bem mais claras.", tone: "mint" },
  { id: 3, name: "Você", initials: "VC", role: "Member", time: "09:47", text: "Perfeito. Também deixei uma nota no canal #feedback sobre os estados vazios.", tone: "yellow" },
];
const members = [
  { name: "Maya Chen", role: "Product lead", initials: "MC", status: "Online", tone: "orange" },
  { name: "Ravi Mendes", role: "Designer", initials: "RM", status: "Online", tone: "mint" },
  { name: "Joana Silva", role: "Engineer", initials: "JS", status: "Ausente", tone: "yellow" },
  { name: "Theo Martins", role: "Community", initials: "TM", status: "Ocupado", tone: "pink" },
];

function Brand({ compact = false }) {
  return <div className={`brand ${compact ? "brand-compact" : ""}`} data-testid="vexor-brand"><span className="brand-mark">V</span><span><strong>VEXOR</strong>{!compact && <small>CONNECT. TALK. SHARE.</small>}</span></div>;
}

function Landing({ onEnter }) {
  return <main className="landing" data-testid="landing-page">
    <nav className="landing-nav"><Brand /><div className="landing-links"><a href="#platform" data-testid="platform-link">Plataforma</a><a href="#communities" data-testid="communities-link">Comunidades</a><a href="#security" data-testid="security-link">Segurança</a></div><button className="button button-ghost" onClick={onEnter} data-testid="landing-login-button">Entrar <LogIn size={15}/></button></nav>
    <section className="hero"><div className="hero-copy"><p className="eyebrow"><span className="live-dot"/> SINAL ABERTO · 01</p><h1>Conexões que<br/><em>movem</em> ideias.</h1><p className="hero-lede">Uma nova forma de se conectar, conversar e compartilhar — para comunidades que fazem acontecer.</p><div className="hero-actions"><button className="button button-primary" onClick={onEnter} data-testid="landing-start-button">Começar agora <Zap size={16}/></button><a className="text-link" href="#platform" data-testid="landing-explore-link">Explorar a VEXOR <span>↗</span></a></div><div className="hero-proof"><div className="avatar-stack"><span className="avatar avatar-orange">MC</span><span className="avatar avatar-mint">RM</span><span className="avatar avatar-yellow">JS</span><span className="avatar avatar-dark">+2k</span></div><span><strong>2,418</strong> pessoas conectadas<br/><small>em comunidades VEXOR agora</small></span></div></div><div className="hero-art" aria-label="Prévia do workspace VEXOR"><div className="art-grid"/><div className="signal-card"><div className="signal-header"><span className="signal-bars"><i/><i/><i/></span><span>LIVE / NEXUS LAB</span><span className="signal-time">09:48:12</span></div><div className="waveform"><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/><i/></div><div className="signal-footer"><strong>Uma sala para cada conversa.</strong><span><Signal size={13}/> 24 membros online</span></div></div><div className="floating-note"><span className="live-dot"/> VOZ EM TEMPO REAL<br/><strong>Conecte-se sem ruído.</strong></div></div></section>
    <section className="feature-strip" id="platform"><div><span className="feature-number">01</span><h3>Comunidades vivas</h3><p>Espaços com contexto, canais e pessoas que importam.</p></div><div><span className="feature-number">02</span><h3>Conversa sem fricção</h3><p>Mensagens rápidas, presença clara e foco no que importa.</p></div><div><span className="feature-number">03</span><h3>Pronto para o próximo sinal</h3><p>Voz e compartilhamento de tela preparados para a próxima fase.</p></div></section>
  </main>;
}

function Auth({ onBack, onSuccess }) {
  const [isLogin, setIsLogin] = useState(true); const [recovery, setRecovery] = useState(false); const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  const submit = async (event) => { event.preventDefault(); setError(""); setLoading(true); const form = new FormData(event.currentTarget); try { const body = { email: form.get("email"), password: form.get("password"), ...(form.get("username") ? { username: form.get("username") } : {}) }; const data = await api(isLogin ? "/auth/login" : "/auth/register", { method: "POST", body: JSON.stringify(body) }); saveSession(data); onSuccess(data.user); } catch (err) { setError(err.message); } finally { setLoading(false); } };
  const recover = async (event) => { event.preventDefault(); setError(""); const form = new FormData(event.currentTarget); try { if (!form.get("code")) { const data = await api("/auth/recovery/request", { method: "POST", body: JSON.stringify({ email: form.get("email") }) }); setError(`Código de demonstração: ${data.demo_code}`); } else { await api("/auth/recovery/verify", { method: "POST", body: JSON.stringify({ email: form.get("email"), code: form.get("code"), new_password: form.get("password") }) }); setRecovery(false); setError("Senha atualizada. Faça login novamente."); } } catch (err) { setError(err.message); } };
  return <div className="auth-page" data-testid="auth-page"><button className="auth-back" onClick={onBack} data-testid="auth-back-button"><X size={16}/> Voltar</button><div className="auth-aside"><Brand/><p className="eyebrow">SUA SALA DE CONTROLE</p><h1>Onde as boas<br/><em>conversas</em><br/>começam.</h1><p>Entre na sua rede de pessoas e projetos.</p></div><section className="auth-panel"><div className="auth-heading"><p className="eyebrow">{recovery ? "RECUPERAÇÃO SEGURA" : isLogin ? "BEM-VINDO DE VOLTA" : "COMECE AGORA"}</p><h2>{recovery ? "Recupere seu acesso." : isLogin ? "Acesse seu espaço." : "Crie seu espaço."}</h2><p>{recovery ? "Use o código enviado para criar uma nova senha." : isLogin ? "Continue de onde você parou." : "Seu próximo capítulo começa aqui."}</p></div><form onSubmit={recovery ? recover : submit}><label>E-mail<input name="email" type="email" placeholder="voce@exemplo.com" required data-testid="auth-email-input"/></label>{recovery && <label>Código<input name="code" placeholder="deixe vazio para gerar" data-testid="recovery-code-input"/></label>}<label>{recovery ? "Nova senha" : "Senha"}<input name="password" type="password" placeholder="mínimo 8 caracteres" minLength="8" required data-testid="auth-password-input"/></label>{!isLogin && !recovery && <label>Nome de usuário<input name="username" placeholder="seu_nome" required data-testid="auth-username-input"/></label>}{error && <p className="form-error" data-testid="auth-error">{error}</p>}<button className="button button-primary full" type="submit" disabled={loading} data-testid="auth-submit-button">{recovery ? "Continuar recuperação" : loading ? "Conectando..." : isLogin ? "Entrar na VEXOR" : "Criar conta"}<LogIn size={16}/></button></form>{!recovery && <><div className="auth-divider"><span>ou</span></div><button className="button button-outline full disabled" disabled data-testid="google-auth-button">Continuar com Google <small>EM BREVE</small></button></>}<p className="auth-switch">{recovery ? "Lembrou a senha?" : isLogin ? "Ainda não tem conta?" : "Já tem uma conta?"} <button onClick={() => { setRecovery(!recovery); setIsLogin(true); setError(""); }} data-testid="auth-recovery-toggle">{recovery ? "Entrar" : "Esqueci minha senha"}</button>{!recovery && <>{" · "}<button onClick={() => { setIsLogin(!isLogin); setError(""); }} data-testid="auth-mode-toggle">{isLogin ? "Criar agora" : "Entrar"}</button></>}</p></section></div>;
}

function Workspace({ onLogout }) {
  const [activeCommunity, setActiveCommunity] = useState("nexus"); const [activeChannel, setActiveChannel] = useState("general"); const [communityList, setCommunityList] = useState(communities); const [channelList, setChannelList] = useState(channels); const [messages, setMessages] = useState(initialMessages); const [draft, setDraft] = useState(""); const [mobileRail, setMobileRail] = useState(false);
  const [connection, setConnection] = useState("Conectando"); const [showVoice, setShowVoice] = useState(false); const [panel, setPanel] = useState(null); const socketRef = useRef(null);
  const community = communityList.find((item) => item.id === activeCommunity) || communityList[0]; const channelName = channelList.find((item) => item.id === activeChannel)?.name || channelList.find((item) => item.id === activeChannel)?.label || activeChannel;
  useEffect(() => { api("/communities").then((items) => { if (items.length) { setCommunityList(items.map((item) => ({ ...item, label: item.name.slice(0, 1).toUpperCase(), color: "orange" }))); setActiveCommunity(items[0].id); } }).catch(() => {}); }, []);
  useEffect(() => { if (!activeCommunity || activeCommunity === "nexus") return; api(`/communities/${activeCommunity}/channels`).then((items) => { if (items.length) { setChannelList(items); setActiveChannel(items[0].id); } }).catch(() => {}); }, [activeCommunity]);
  useEffect(() => { if (!activeChannel || activeChannel === "general") return; api(`/channels/${activeChannel}/messages`).then((items) => { if (items.length) setMessages(items.map((item) => ({ ...item, name: item.author, initials: "VC", role: "Member", time: new Date(item.created_at).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" }), tone: "yellow" }))); }).catch(() => {}); }, [activeChannel]);
  useEffect(() => { const openPanel = (event) => setPanel(event.detail); window.addEventListener("vexor:panel", openPanel); return () => window.removeEventListener("vexor:panel", openPanel); }, []);
  useEffect(() => {
    const backend = process.env.REACT_APP_BACKEND_URL;
    const socketUrl = websocketUrl(activeChannel);
    const socket = new WebSocket(socketUrl); socketRef.current = socket;
    socket.onopen = () => { if (socketRef.current === socket) setConnection("Conectado"); };
    socket.onclose = () => { if (socketRef.current === socket) setConnection("Offline"); };
    socket.onerror = () => { if (socketRef.current === socket) setConnection("Offline"); };
    socket.onmessage = (event) => { const payload = JSON.parse(event.data); if (payload.type === "message") setMessages((prev) => [...prev, { id: Date.now() + Math.random(), name: payload.author, initials: "VC", role: "Member", time: "agora", text: payload.text, tone: "yellow" }]); };
    return () => { if (socketRef.current === socket) socketRef.current = null; socket.close(); };
  }, [activeChannel]);
  const sendMessage = (event) => { event.preventDefault(); if (!draft.trim()) return; const text = draft.trim(); if (socketRef.current?.readyState === WebSocket.OPEN) socketRef.current.send(JSON.stringify({ type: "message", text, author: "Você" })); else setMessages((prev) => [...prev, { id: Date.now(), name: "Você", initials: "VC", role: "Member", time: "agora", text, tone: "yellow" }]); setDraft(""); };
  return <main className="workspace" data-testid="workspace-page"><aside className={`community-rail ${mobileRail ? "mobile-open" : ""}`}><Brand compact/><div className="rail-rule"/><button className="rail-item active" data-testid="home-rail-button"><Zap size={18}/></button>{communityList.map((item) => <button key={item.id} onClick={() => { setActiveCommunity(item.id); setMobileRail(false); }} className={`community-icon ${item.color} ${item.id === activeCommunity ? "selected" : ""}`} aria-label={item.name} data-testid={`community-${item.id}-button`}>{item.label}</button>)}<button className="community-icon add" onClick={() => setPanel("community")} data-testid="add-community-button"><Plus size={20}/></button><div className="rail-bottom"><button className="rail-item" data-testid="notifications-rail-button"><Bell size={18}/></button><button className="rail-item" onClick={() => { clearSession(); onLogout(); }} data-testid="logout-rail-button"><LogIn size={18}/></button></div></aside><aside className="channel-rail"><div className="community-heading"><div><span className="eyebrow">COMUNIDADE</span><strong>{community.name}</strong></div><button onClick={() => setPanel("audit")} data-testid="community-menu-button"><MoreHorizontal size={18}/></button></div><div className="channel-search"><Search size={15}/><input placeholder="Buscar" aria-label="Buscar canais" data-testid="channel-search-input"/></div><div className="channel-section"><span className="section-label">CANAIS DE TEXTO <button onClick={() => setPanel("channel")} data-testid="add-channel-button"><Plus size={14}/></button></span>{channelList.map((item) => <button key={item.id} onClick={() => setActiveChannel(item.id)} className={`channel-item ${item.id === activeChannel ? "active" : ""}`} data-testid={`channel-${item.id}-button`}><Hash size={16}/>{item.name || item.label}{item.count && <small>{item.count}</small>}</button>)}</div><div className="channel-section"><span className="section-label">SALAS DE VOZ <button onClick={() => setShowVoice(true)} data-testid="add-voice-room-button"><Plus size={14}/></button></span><button className="channel-item voice-ready" onClick={() => setShowVoice(true)} data-testid="voice-room-lounge-button"><Volume2 size={16}/> lounge <span className="ready-pill">AO VIVO</span></button></div><div className="user-dock"><span className="avatar avatar-yellow">VC</span><div><strong>você</strong><small><span className="status-dot"/> online</small></div><button data-testid="user-settings-button"><Settings size={16}/></button></div></aside><section className="conversation"><header className="topbar"><button className="mobile-menu" onClick={() => setMobileRail(!mobileRail)} data-testid="mobile-menu-button"><Menu size={20}/></button><div className="channel-title"><Hash size={20}/><div><strong>{channelName}</strong><span>Conversa da comunidade</span></div></div><div className="top-actions"><span className="connection-status" data-testid="connection-status"><span className="status-dot"/> {connection}</span><button onClick={() => setPanel("invite")} data-testid="invite-button"><UserPlus size={17}/><span>Convidar</span></button><button aria-label="Buscar mensagens" data-testid="message-search-button"><Search size={18}/></button></div></header><div className="thread" data-testid="message-thread"><div className="thread-intro"><span className="channel-symbol">#</span><h2>Bem-vindo ao #{channelName}</h2><p>Este é o início do canal. Compartilhe ideias, atualizações e links com a comunidade.</p></div>{messages.map((message) => <article className="message" key={message.id} data-testid={`message-${message.id}`}><span className={`avatar avatar-${message.tone}`}>{message.initials}</span><div className="message-content"><div className="message-meta"><strong>{message.name}</strong><span className="role-tag">{message.role}</span><time>{message.time}</time></div><p>{message.text}</p></div><button className="message-more" aria-label="Mais ações" data-testid={`message-actions-${message.id}`}><MoreHorizontal size={16}/></button></article>)}</div><form className="composer" onSubmit={sendMessage}><button type="button" aria-label="Adicionar arquivo" data-testid="attach-message-button"><Plus size={19}/></button><input value={draft} onChange={(e) => setDraft(e.target.value)} placeholder={`Mensagem em #${channelName}`} data-testid="message-composer-input"/><button type="button" aria-label="Adicionar emoji" data-testid="emoji-message-button"><Smile size={18}/></button><button className="send-button" type="submit" aria-label="Enviar mensagem" data-testid="send-message-button"><Send size={17}/></button></form></section><aside className="member-rail"><div className="member-header"><span className="eyebrow">PRESENÇA</span><strong><span className="status-dot"/> 4 online</strong><button onClick={() => setPanel("moderation")} data-testid="moderation-open-button"><Shield size={14}/> Moderar</button></div><div className="member-group"><span className="section-label">ONLINE — 3</span>{members.slice(0, 2).map((member) => <Member member={member} key={member.name}/>)}</div><div className="member-group"><span className="section-label">AUSENTE / OCUPADO</span>{members.slice(2).map((member) => <Member member={member} key={member.name}/>)}</div><div className="voice-dock" data-testid="voice-ready-card"><div className="voice-dock-icon"><Headphones size={17}/></div><div><strong>Voz e tela</strong><p>Microfone e P2P</p></div><button onClick={() => setShowVoice(true)} aria-label="Abrir sala de voz" data-testid="voice-settings-button"><Settings size={15}/></button><small>WEBRTC P2P</small></div></aside>{showVoice && <VoiceRoom room={activeChannel} onClose={() => setShowVoice(false)}/>} {panel && <ManagementPanel mode={panel} communityId={activeCommunity} onClose={() => setPanel(null)} onResult={(result) => { if (result.type === "community") { setCommunityList((items) => [...items, { ...result.data, label: result.data.name.slice(0, 1).toUpperCase(), color: "orange" }]); setActiveCommunity(result.data.id); } if (result.type === "channel") { setChannelList((items) => [...items, result.data]); setActiveChannel(result.data.id); } }}/>}</main>;
}

function Member({ member }) { return <div className="member-row" data-testid={`member-${member.initials.toLowerCase()}`}><span className={`avatar avatar-${member.tone}`}>{member.initials}</span><div><strong>{member.name}</strong><small><span className={`status-dot ${member.status !== "Online" ? "muted" : ""}`}/> {member.status}</small></div></div>; }

function GlobalTools() { const open = (detail) => window.dispatchEvent(new CustomEvent("vexor:panel", { detail })); return <div className="global-tools" data-testid="global-tools"><button onClick={() => open("friends")} data-testid="global-friends-button"><Users size={15}/> Amigos / DMs</button><button onClick={() => open("rules")} data-testid="global-rules-button"><Shield size={15}/> Regras</button></div>; }

function CommunitySocialPanel({ mode, communityId, onClose }) {
  const [query, setQuery] = useState(""); const [results, setResults] = useState([]); const [rules, setRules] = useState(""); const [dmTarget, setDmTarget] = useState(""); const [dmText, setDmText] = useState(""); const [message, setMessage] = useState("");
  useEffect(() => { if (mode === "rules") api(`/communities/${communityId}/rules`).then((data) => setRules(data.rules.join("\n"))).catch((error) => setMessage(error.message)); }, [mode, communityId]);
  const search = async (event) => { event.preventDefault(); try { setResults(await api(`/users/search?q=${encodeURIComponent(query)}`)); } catch (error) { setMessage(error.message); } };
  const sendRequest = async (id) => { try { await api(`/friends/${id}`, { method: "POST" }); setMessage("Solicitação enviada"); } catch (error) { setMessage(error.message); } };
  const saveRules = async (event) => { event.preventDefault(); try { await api(`/communities/${communityId}/rules`, { method: "PUT", body: JSON.stringify({ rules: rules.split("\n").map((item) => item.trim()).filter(Boolean) }) }); await api(`/communities/${communityId}/rules/accept`, { method: "POST" }); setMessage("Regras salvas e aceitas"); } catch (error) { setMessage(error.message); } };
  const sendDm = async (event) => { event.preventDefault(); try { await api(`/dm/${dmTarget}`, { method: "POST", body: JSON.stringify({ text: dmText }) }); setDmText(""); setMessage("Mensagem privada enviada"); } catch (error) { setMessage(error.message); } };
  return <div className="voice-modal" data-testid="social-panel"><div className="voice-modal-card management-card"><button className="voice-close" onClick={onClose} data-testid="social-close-button"><X size={18}/></button><span className="eyebrow">REDE VEXOR</span><h2>{mode === "rules" ? "Regras da comunidade" : "Amigos e DMs"}</h2>{mode === "rules" ? <form onSubmit={saveRules}><label>Uma regra por linha<textarea value={rules} onChange={(event) => setRules(event.target.value)} rows="7" data-testid="community-rules-input"/></label><button className="button button-primary full" type="submit" data-testid="community-rules-save-button">Salvar e aceitar <Shield size={16}/></button></form> : <><form onSubmit={search}><label>Buscar usuário<input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="nome de usuário" data-testid="friend-search-input"/></label><button className="button button-primary full" type="submit" data-testid="friend-search-button">Buscar <Users size={16}/></button></form><div className="search-results">{results.map((item) => <p key={item.id}><strong>{item.username}</strong><button onClick={() => sendRequest(item.id)} data-testid={`friend-request-${item.id}`}>Adicionar</button></p>)}</div><form onSubmit={sendDm}><label>ID do amigo<input value={dmTarget} onChange={(event) => setDmTarget(event.target.value)} required data-testid="dm-target-input"/></label><label>Mensagem<input value={dmText} onChange={(event) => setDmText(event.target.value)} required data-testid="dm-message-input"/></label><button className="button button-outline full" type="submit" data-testid="dm-send-button">Enviar DM <Send size={16}/></button></form></>}{message && <p className="form-success" data-testid="social-result">{message}</p>}</div></div>;
}

function ManagementPanel({ mode, communityId, onClose, onResult }) {
  const [name, setName] = useState(""); const [reason, setReason] = useState(""); const [target, setTarget] = useState(""); const [action, setAction] = useState("ban"); const [result, setResult] = useState(""); const [logs, setLogs] = useState([]);
  useEffect(() => { if (mode === "audit") api(`/communities/${communityId}/audit-log`).then(setLogs).catch((error) => setResult(error.message)); }, [mode, communityId]);
  if (mode === "friends" || mode === "rules") return <CommunitySocialPanel mode={mode} communityId={communityId} onClose={onClose} />;
  const submit = async (event) => { event.preventDefault(); try { let data; if (mode === "community") data = await api("/communities", { method: "POST", body: JSON.stringify({ name, description: "Comunidade criada pela VEXOR" }) }); if (mode === "channel") data = await api(`/communities/${communityId}/channels`, { method: "POST", body: JSON.stringify({ name, kind: "text" }) }); if (mode === "invite") data = await api(`/communities/${communityId}/invites`, { method: "POST", body: "{}" }); if (mode === "moderation") data = await api(`/communities/${communityId}/members/${target}/moderate`, { method: "POST", body: JSON.stringify({ action, reason }) }); setResult(mode === "invite" ? `Convite: ${data.id}` : "Alteração salva com sucesso"); if (data?.id && (mode === "community" || mode === "channel")) onResult({ type: mode, data }); } catch (error) { setResult(error.message); } };
  const panelTitle = { community: "Nova comunidade", channel: "Novo canal", invite: "Convite da comunidade", moderation: "Moderação", audit: "Audit log" }[mode];
  return <div className="voice-modal" data-testid="management-panel"><div className="voice-modal-card management-card"><button className="voice-close" onClick={onClose} data-testid="management-close-button"><X size={18}/></button><span className="eyebrow">CONTROLE VEXOR</span><h2>{panelTitle}</h2>{mode === "audit" ? <div className="audit-list">{logs.length ? logs.map((log) => <p key={log.id} data-testid="audit-log-entry"><strong>{log.action}</strong><small>{log.reason || "sem motivo"}</small></p>) : <p className="empty-state">Nenhuma ação registrada ainda.</p>}</div> : <form onSubmit={submit}>{(mode === "community" || mode === "channel") && <label>Nome<input value={name} onChange={(event) => setName(event.target.value)} required minLength="2" data-testid={`${mode}-name-input`}/></label>}{mode === "moderation" && <><label>ID do usuário<input value={target} onChange={(event) => setTarget(event.target.value)} required data-testid="moderation-target-input"/></label><label>Ação<select value={action} onChange={(event) => setAction(event.target.value)} data-testid="moderation-action-select"><option value="ban">Banir</option><option value="kick">Expulsar</option><option value="unban">Remover banimento</option></select></label><label>Motivo<input value={reason} onChange={(event) => setReason(event.target.value)} data-testid="moderation-reason-input"/></label></>}{mode === "invite" && <p className="empty-state">Gere um link de convite válido por 72 horas para compartilhar com sua comunidade.</p>}{mode !== "moderation" && mode !== "invite" || mode === "moderation" ? <button className="button button-primary full" type="submit" data-testid="management-submit-button">Salvar <Shield size={16}/></button> : <button className="button button-primary full" type="submit" data-testid="management-submit-button">Gerar convite <UserPlus size={16}/></button>}</form>}{result && <p className="form-success" data-testid="management-result">{result}</p>}</div></div>;
}

function VoiceRoom({ room, onClose }) {
  const [state, setState] = useState("Preparando microfone"); const [muted, setMuted] = useState(false); const [sharing, setSharing] = useState(false); const [speaking, setSpeaking] = useState(false); const [speakingName, setSpeakingName] = useState(""); const [peers, setPeers] = useState(0); const streamRef = useRef(null); const screenRef = useRef(null); const socketRef = useRef(null); const idRef = useRef(`peer-${Math.random().toString(36).slice(2)}`); const peersRef = useRef({});
  useEffect(() => {
    let mounted = true;
    const start = async () => {
      try { const stream = await navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true } }); streamRef.current = stream; const socket = new WebSocket(websocketUrl(room)); socketRef.current = socket; socket.onopen = () => { if (mounted) setState("Conectado"); socket.send(JSON.stringify({ type: "voice-join", peer_id: idRef.current })); }; socket.onmessage = async (event) => { const data = JSON.parse(event.data); if (!data.type.startsWith("voice-")) return; if (data.type === "voice-peer-joined" && data.peer_id !== idRef.current) { await createOffer(data.peer_id); setPeers((count) => count + 1); } if (data.target_id && data.target_id !== idRef.current) return; if (data.type === "voice-offer") { const peer = await createPeer(data.peer_id); await peer.setRemoteDescription(data.description); const answer = await peer.createAnswer(); await peer.setLocalDescription(answer); socket.send(JSON.stringify({ type: "voice-answer", peer_id: idRef.current, target_id: data.peer_id, description: answer })); } if (data.type === "voice-answer" && peersRef.current[data.peer_id]) await peersRef.current[data.peer_id].setRemoteDescription(data.description); if (data.type === "voice-ice" && peersRef.current[data.peer_id]) await peersRef.current[data.peer_id].addIceCandidate(data.candidate); }; } catch (error) { if (mounted) setState(error.name === "NotAllowedError" ? "Microfone bloqueado" : "Microfone indisponível"); }
    };
    const createPeer = async (peerId) => { const peer = new RTCPeerConnection(); streamRef.current?.getTracks().forEach((track) => peer.addTrack(track, streamRef.current)); peer.addTransceiver("video", { direction: "sendrecv" }); peer.ontrack = (event) => { const audio = new Audio(); audio.autoplay = true; audio.srcObject = event.streams[0]; audio.play().catch(() => {}); setPeers((count) => Math.max(count, 1)); setSpeakingName("Participante remoto"); setSpeaking(true); window.setTimeout(() => { setSpeaking(false); setSpeakingName(""); }, 900); }; peer.onicecandidate = (event) => { if (event.candidate) socketRef.current?.send(JSON.stringify({ type: "voice-ice", peer_id: idRef.current, target_id: peerId, candidate: event.candidate })); }; peersRef.current[peerId] = peer; return peer; };
    const createOffer = async (peerId) => { const peer = await createPeer(peerId); const offer = await peer.createOffer(); await peer.setLocalDescription(offer); socketRef.current?.send(JSON.stringify({ type: "voice-offer", peer_id: idRef.current, target_id: peerId, description: offer })); };
    start(); const activePeers = peersRef.current; return () => { mounted = false; streamRef.current?.getTracks().forEach((track) => track.stop()); Object.values(activePeers).forEach((peer) => peer.close()); socketRef.current?.close(); };
  }, [room]);
  const toggleMute = () => { const track = streamRef.current?.getAudioTracks()[0]; if (track) { track.enabled = !track.enabled; setMuted(!track.enabled); } };
  const toggleShare = async () => { if (sharing) { screenRef.current?.getTracks().forEach((track) => track.stop()); setSharing(false); socketRef.current?.send(JSON.stringify({ type: "screen-stop", peer_id: idRef.current })); return; } try { const screen = await navigator.mediaDevices.getDisplayMedia({ video: true, audio: true }); screenRef.current = screen; const screenTrack = screen.getVideoTracks()[0]; Object.values(peersRef.current).forEach((peer) => { const sender = peer.getSenders().find((item) => item.track?.kind === "video"); if (sender) sender.replaceTrack(screenTrack); }); screenTrack.onended = () => { setSharing(false); socketRef.current?.send(JSON.stringify({ type: "screen-stop", peer_id: idRef.current })); }; setSharing(true); socketRef.current?.send(JSON.stringify({ type: "screen-start", peer_id: idRef.current, has_audio: screen.getAudioTracks().length > 0 })); } catch (error) { setState("Compartilhamento cancelado"); } };
  return <div className="voice-modal" data-testid="voice-room-modal"><div className="voice-modal-card"><button className="voice-close" onClick={onClose} data-testid="voice-close-button"><X size={18}/></button><span className="eyebrow">SALA DE VOZ · P2P</span><h2>lounge</h2><p className="voice-state"><span className="status-dot"/> {state}</p><div className="voice-meter"><Mic size={22}/><span>{peers + 1} participante{peers !== 0 ? "s" : ""} {speaking && <b className="speaking-label">· {speakingName || "falando"}</b>}</span></div><div className="voice-controls"><button className={muted ? "muted-control" : ""} onClick={toggleMute} data-testid="voice-mute-button"><Mic size={18}/>{muted ? "Ativar microfone" : "Silenciar"}</button><button className={sharing ? "muted-control" : ""} onClick={toggleShare} data-testid="screen-share-button"><MonitorUp size={18}/>{sharing ? "Parar tela" : "Compartilhar tela"}</button><button onClick={onClose} data-testid="voice-leave-button"><LogIn size={18}/> Sair da sala</button></div><small className="voice-note">Áudio remoto P2P · áudio do sistema conforme suporte do navegador</small></div></div>;
}

export default function App() { const [screen, setScreen] = useState("landing"); return screen === "landing" ? <Landing onEnter={() => setScreen("auth")}/> : screen === "auth" ? <Auth onBack={() => setScreen("landing")} onSuccess={() => setScreen("workspace")}/> : <><Workspace onLogout={() => setScreen("landing")}/><GlobalTools/></>; }